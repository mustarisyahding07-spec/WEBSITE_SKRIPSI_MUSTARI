<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>UMKM Ivo Karya - Abon Ikan & Sapi Khas Sidrap</title>
        
        <!-- Fonts -->
        <link rel="preconnect" href="https://fonts.bunny.net">
        <link href="https://fonts.bunny.net/css?family=figtree:400,600&display=swap" rel="stylesheet" />

        <!-- Tailwind CSS (via Vite or CDN backup) -->
        <!-- Using CDN for immediate preview reliability if build is pending -->
        <script src="https://cdn.tailwindcss.com"></script>
        <script>
            tailwind.config = {
                theme: {
                    extend: {
                        colors: {
                            brand: {
                                500: '#8B4513', // Saddle Brown
                                600: '#723A0F',
                            },
                            beige: {
                                100: '#F5F5DC', // Beige
                                200: '#EBEBC8',
                            }
                        }
                    }
                }
            }
        </script>
        
        <style>
            .hero-pattern {
                background-color: #F5F5DC;
                background-image: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%238B4513' fill-opacity='0.05'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
            }
        </style>
    </head>
    <body class="antialiased bg-gray-50 text-gray-800">
        
        <!-- Navigation -->
        <nav class="bg-white shadow-sm fixed w-full z-10 top-0">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="flex justify-between h-16">
                    <div class="flex items-center">
                        <a href="/" class="flex-shrink-0 flex items-center">
                            <!-- Logo Placeholder -->
                            <span class="font-bold text-2xl text-brand-500">Ivo Karya</span>
                        </a>
                    </div>
                    
                    <!-- Search Bar (Desktop) -->
                    <div class="hidden md:flex flex-1 items-center justify-center px-8">
                        <div class="w-full max-w-lg relative">
                            <input type="text" class="w-full rounded-full border border-gray-300 px-4 py-2 pl-10 focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent text-sm" placeholder="Cari produk abon...">
                            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
                            </div>
                        </div>
                    </div>

                    <div class="flex items-center space-x-4">
                        <a href="{{ route('cart.index') }}" class="text-gray-500 hover:text-brand-500 relative">
                            <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z"></path></svg>
                            <!-- Cart Badge -->
                            @if(session('cart') && count(session('cart')) > 0)
                                <span class="absolute -top-1 -right-1 bg-red-500 text-white rounded-full text-xs w-4 h-4 flex items-center justify-center">{{ count(session('cart')) }}</span>
                            @endif
                        </a>
                        
                        @auth
                            <a href="{{ url('/dashboard') }}" class="text-sm font-medium text-gray-700 hover:text-brand-500">Dashboard</a>
                        @else
                            <a href="{{ route('login') }}" class="text-sm font-medium text-gray-700 hover:text-brand-500">Log in</a>
                            <!-- Register link removed as per cleanliness -->
                        @endauth
                    </div>
                </div>
            </div>
        </nav>

        <!-- Hero Section -->
        <header class="hero-pattern pt-24 pb-12 md:pt-32 md:pb-24">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex flex-col md:flex-row items-center">
                <div class="md:w-1/2 mb-10 md:mb-0 text-center md:text-left">
                    <h1 class="text-4xl md:text-5xl font-extrabold text-gray-900 leading-tight mb-4">
                        Cita Rasa Abon Ikan <br><span class="text-brand-500">Khas Sidenreng Rappang</span>
                    </h1>
                    <p class="text-lg text-gray-600 mb-8 max-w-lg mx-auto md:mx-0">
                        Nikmati kelezatan abon ikan dan sapi premium olahan UMKM Ivo Karya. Halal, higienis, dan tanpa pengawet.
                    </p>
                    <a href="#products" class="inline-block bg-brand-500 hover:bg-brand-600 text-white font-bold py-3 px-8 rounded-full shadow-lg transition duration-300 transform hover:-translate-y-1">
                        Belanja Sekarang
                    </a>
                </div>
                <!-- Hero Image Area -->
                <div class="md:w-1/2 flex justify-center">
                    <!-- Placeholder or dynamic image -->
                    <div class="w-72 h-72 md:w-96 md:h-96 bg-gray-200 rounded-full flex items-center justify-center shadow-2xl relative overflow-hidden">
                        <img src="https://images.unsplash.com/photo-1628151016143-5eoo8f760d65?q=80&w=1000&auto=format&fit=crop" alt="Abon Ikan" class="w-full h-full object-cover opacity-90">
                        <div class="absolute inset-0 bg-brand-500 opacity-10 mix-blend-multiply"></div>
                    </div>
                </div>
            </div>
        </header>

        <!-- Product Grid -->
        <main id="products" class="py-16 bg-white">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="flex justify-between items-center mb-10">
                    <h2 class="text-3xl font-bold text-gray-900">Produk Pilihan</h2>
                    <div class="space-x-2">
                        <a href="{{ route('home') }}" class="px-4 py-2 rounded-full text-sm font-medium {{ !request('category') ? 'bg-brand-500 text-white' : 'bg-gray-100 text-gray-700 hover:bg-gray-200' }}">Semua</a>
                        <a href="{{ route('home', ['category' => 'abon_ikan']) }}" class="px-4 py-2 rounded-full text-sm font-medium {{ request('category') == 'abon_ikan' ? 'bg-brand-500 text-white' : 'bg-gray-100 text-gray-700 hover:bg-gray-200' }}">Abon Ikan</a>
                        <a href="{{ route('home', ['category' => 'abon_sapi']) }}" class="px-4 py-2 rounded-full text-sm font-medium {{ request('category') == 'abon_sapi' ? 'bg-brand-500 text-white' : 'bg-gray-100 text-gray-700 hover:bg-gray-200' }}">Abon Sapi</a>
                    </div>
                </div>

                @if($products->isEmpty())
                    <div class="text-center py-20">
                        <p class="text-gray-500 text-lg">Belum ada produk yang tersedia saat ini.</p>
                    </div>
                @else
                    <div class="grid grid-cols-2 md:grid-cols-4 gap-6 md:gap-8">
                        @foreach($products as $product)
                            <div class="bg-white rounded-xl shadow-sm hover:shadow-md transition duration-300 border border-gray-100 overflow-hidden group">
                                <div class="relative h-48 md:h-56 bg-gray-100 overflow-hidden">
                                    @if($product->image)
                                        <img src="{{ Storage::url($product->image) }}" alt="{{ $product->name }}" class="w-full h-full object-cover group-hover:scale-105 transition duration-500">
                                    @else
                                        <div class="flex items-center justify-center h-full text-gray-400">
                                            <svg class="h-12 w-12" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path></svg>
                                        </div>
                                    @endif
                                    
                                    <!-- Badge Weight -->
                                    <div class="absolute top-2 right-2 bg-white bg-opacity-90 backdrop-filter backdrop-blur px-2 py-1 rounded-md text-xs font-bold text-gray-700 shadow-sm">
                                        {{ $product->weight }} gr
                                    </div>
                                </div>
                                <div class="p-4">
                                    <div class="text-xs text-brand-500 font-semibold mb-1 uppercase tracking-wider">{{ str_replace('_', ' ', $product->category) }}</div>
                                    <h3 class="text-gray-900 font-bold text-lg mb-1 leading-tight">{{ $product->name }}</h3>
                                    <div class="text-gray-500 text-sm mb-3 line-clamp-2">{{ $product->description }}</div>
                                    
                                    <div class="flex items-center justify-between mt-4">
                                        <div class="text-gray-900 font-bold">
                                            Rp {{ number_format($product->price, 0, ',', '.') }}
                                        </div>
                                        <a href="{{ route('cart.add', $product->id) }}" class="bg-gray-900 hover:bg-black text-white p-2 rounded-full transition duration-300 transform active:scale-95" title="Add to Cart">
                                            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path></svg>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        @endforeach
                    </div>
                @endif
            </div>
        </main>

        <!-- Footer -->
        <footer class="bg-gray-900 text-white py-12">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="flex flex-col md:flex-row justify-between items-center">
                    <div class="mb-6 md:mb-0 text-center md:text-left">
                        <span class="font-bold text-2xl text-white">Ivo Karya</span>
                        <p class="text-gray-400 mt-2 text-sm">Delicious Fish & Beef Floss from Sidenreng Rappang.</p>
                    </div>
                    <div class="flex space-x-6">
                        <a href="#" class="text-gray-400 hover:text-white transition">Instagram</a>
                        <a href="#" class="text-gray-400 hover:text-white transition">Facebook</a>
                        <a href="#" class="text-gray-400 hover:text-white transition">WhatsApp</a>
                    </div>
                </div>
                <div class="mt-8 border-t border-gray-800 pt-8 text-center text-gray-500 text-sm">
                    &copy; 2026 UMKM Ivo Karya. All rights reserved.
                </div>
            </div>
        </footer>

    </body>
</html>
